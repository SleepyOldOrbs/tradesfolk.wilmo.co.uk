---
phase: 01-agent-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/javascript-developer.md
  - agents/react-specialist.md
  - agents/ux-designer.md
autonomous: true
requirements:
  - AGNT-01
  - AGNT-02
  - AGNT-05

must_haves:
  truths:
    - "javascript-developer description has 3 <example> blocks with JS/TS/Node tasks that avoid React/Next.js territory"
    - "react-specialist description has 3 <example> blocks with React/Next.js tasks distinct from general JS work"
    - "ux-designer description has 3 <example> blocks covering UI design, accessibility, and design systems"
    - "All 3 agents have explicit tools frontmatter field with comma-separated tool names"
    - "No two agents have overlapping example scenarios that would confuse delegation"
  artifacts:
    - path: "agents/javascript-developer.md"
      provides: "Frontend agent: JS/TS/Node specialist with implementation tool tier"
      contains: "tools:"
    - path: "agents/react-specialist.md"
      provides: "Frontend agent: React/Next.js specialist with implementation tool tier"
      contains: "tools:"
    - path: "agents/ux-designer.md"
      provides: "Frontend agent: UX/UI specialist with documentation tool tier"
      contains: "tools:"
  key_links:
    - from: "agents/javascript-developer.md"
      to: "agents/react-specialist.md"
      via: "delegation boundary"
      pattern: "javascript-developer handles vanilla JS/TS/Node, react-specialist handles React/Next.js framework work"
---

<objective>
Rewrite the 3 frontend domain agents (javascript-developer, react-specialist, ux-designer) with production-quality descriptions, tool restrictions, permission modes, model assignments, and full system prompt rewrites.

Purpose: These 3 agents share the blue colour domain and have the most overlap risk (JS vs React). Getting their delegation boundaries right is critical for reliable task matching.

Output: 3 fully rewritten agent markdown files with expanded descriptions, `<example>` blocks, `tools` field, and rewritten system prompts.
</objective>

<execution_context>
@/home/dmzserver/.claude/get-shit-done/workflows/execute-plan.md
@/home/dmzserver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-agent-hardening/01-RESEARCH.md
@agents/javascript-developer.md
@agents/react-specialist.md
@agents/ux-designer.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite javascript-developer agent</name>
  <files>agents/javascript-developer.md</files>
  <action>
Completely rewrite `agents/javascript-developer.md` with the following structure:

**Frontmatter changes:**
- `name`: keep as `javascript-developer`
- `model`: keep as `inherit`
- `color`: keep as `blue`
- `description`: Rewrite using the template from research. Must include:
  - Professional summary: "Use this agent for [tasks]. Expert in [domain]. Specializes in [areas]. [Depth hint]."
  - Technology keywords: JavaScript, TypeScript, Node.js, ES2024+, Vite, esbuild, npm, pnpm
  - 3 `<example>` blocks with Context/user/assistant/commentary format
  - Example 1: Explicit request -- e.g., "Add TypeScript strict mode to the API client" (pure TS task, NOT React)
  - Example 2: Different phrasing -- e.g., "Refactor the middleware to use async/await" (Node.js backend task)
  - Example 3: Implicit/proactive -- e.g., "Fix the ESM import issues in the build" (build tooling task)
  - Commentary must clarify boundary: "This is vanilla JS/TS work, not React. Goes to javascript-developer, not react-specialist."
  - Keep under ~600 chars for the summary portion (excluding example blocks)
- Add `tools: Read, Grep, Glob, Write, Edit, Bash, MultiEdit, NotebookEdit` (Implementation tier)
- Add `permissionMode: default`

**System prompt rewrite:**
Keep the three-section structure (Core expertise, Working standards, When given a task) but rewrite for production quality:
- Opening line: "You are a senior JavaScript and TypeScript developer assigned to this team."
- Core expertise: Update to ES2024+, Node.js 22, modern tooling. Include TypeScript 5.x features. Remove React/Next.js mentions (those belong to react-specialist). Focus on: TypeScript strict mode/generics/utility types, ES2024+ features, Node.js (streams, workers, native APIs), build tooling (Vite, esbuild, Turbopack, tsup), testing (Vitest, Node test runner), package management (pnpm, npm workspaces).
- Working standards: Keep existing rules, add: "Prefer ESM over CommonJS", "Use `satisfies` operator for type narrowing"
- When given a task: Keep the 5-step structure, ensure step 1 emphasizes reading existing code first.

**Critical boundary rule:** This agent must NOT mention React, Next.js, or frontend frameworks in its examples. Those belong exclusively to react-specialist.
  </action>
  <verify>
Read the file and confirm:
1. Frontmatter has all fields: name, model, color, description (with 3 example blocks), tools, permissionMode
2. No React/Next.js in examples (delegation boundary maintained)
3. Three-section system prompt structure intact
4. Tools field is comma-separated: `Read, Grep, Glob, Write, Edit, Bash, MultiEdit, NotebookEdit`
  </verify>
  <done>javascript-developer.md is fully rewritten with professional description, 3 task examples covering JS/TS/Node work (not React), implementation-tier tools, and a production-quality system prompt that maintains clear boundaries with react-specialist</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite react-specialist agent</name>
  <files>agents/react-specialist.md</files>
  <action>
Completely rewrite `agents/react-specialist.md` with the following structure:

**Frontmatter changes:**
- `name`: keep as `react-specialist`
- `model`: keep as `inherit`
- `color`: keep as `blue`
- `description`: Rewrite using the template from research. Must include:
  - Professional summary covering React 19, Next.js 15, server components, state management, frontend architecture
  - Technology keywords: React 19, Next.js 15, App Router, RSC, Tailwind CSS, Zustand, TanStack Query
  - 3 `<example>` blocks:
  - Example 1: Explicit -- e.g., "Build a dashboard page with server components and real-time data" (React/Next.js task)
  - Example 2: Different phrasing -- e.g., "Fix the hydration mismatch on the product page" (React-specific debugging)
  - Example 3: Proactive -- e.g., "Optimise the bundle size — the homepage loads too slowly" (React performance)
  - Commentary must clarify: "This is React/Next.js framework work. Goes to react-specialist, not javascript-developer."
- Add `tools: Read, Grep, Glob, Write, Edit, Bash, MultiEdit, NotebookEdit` (Implementation tier)
- Add `permissionMode: default`

**System prompt rewrite:**
- Opening: "You are a senior React and frontend framework specialist assigned to this team."
- Core expertise: React 19 features (server components, server actions, use(), useOptimistic, useFormStatus), Next.js 15 (App Router, RSC, streaming, parallel routes, middleware), state management (Zustand, Jotai, TanStack Query), styling (Tailwind CSS, CSS Modules), performance (profiling, code splitting, Suspense), forms (React Hook Form, Zod), testing (React Testing Library, Vitest, Playwright)
- Working standards: Server components by default, composition over prop drilling, proper memoization, handle all async states, keyboard navigation
- When given a task: 6-step process ending with accessibility verification

**Critical boundary rule:** Examples must be React/Next.js-specific tasks, NOT vanilla JS/TS or general Node.js work.
  </action>
  <verify>
Read the file and confirm:
1. Frontmatter has all fields: name, model, color, description (with 3 example blocks), tools, permissionMode
2. All examples are React/Next.js specific (not vanilla JS)
3. Three-section system prompt structure intact
4. Tools field matches javascript-developer (same Implementation tier)
  </verify>
  <done>react-specialist.md is fully rewritten with professional description, 3 task examples covering React/Next.js work (not vanilla JS), implementation-tier tools, and clear delegation boundary with javascript-developer</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite ux-designer agent</name>
  <files>agents/ux-designer.md</files>
  <action>
Completely rewrite `agents/ux-designer.md` with the following structure:

**Frontmatter changes:**
- `name`: keep as `ux-designer`
- `model`: change to `sonnet` (lightweight agent per research recommendation)
- `color`: keep as `blue`
- `description`: Rewrite using the template:
  - Professional summary covering UI/UX design, accessibility, design systems, responsive layouts
  - Technology keywords: WCAG 2.2, ARIA, Tailwind CSS, Radix UI, design tokens, responsive design
  - 3 `<example>` blocks:
  - Example 1: "Run an accessibility audit on the checkout flow" (accessibility task)
  - Example 2: "Set up a design token system for our component library" (design system task)
  - Example 3: "The mobile layout is broken on the settings page — fix the responsive design" (UI/layout task)
  - Commentary should note: "UX/UI work — design, accessibility, visual polish. Goes to ux-designer."
- Add `tools: Read, Grep, Glob, Write, Edit, Bash` (Documentation tier per user decision)
- Add `permissionMode: default` (research recommended default over plan since ux-designer needs to write CSS/design files)

**System prompt rewrite:**
- Opening: "You are a senior UX/UI designer-developer assigned to this team."
- Core expertise: Design systems (tokens, theming, component APIs), accessibility (WCAG 2.2 AA, ARIA, keyboard nav, screen readers), CSS (grid, container queries, @layer, custom properties), headless UI (Radix, Headless UI), responsive design (mobile-first, fluid typography), interaction design (focus management, states, transitions), colour theory (contrast, colour blindness, dark mode)
- Working standards: Semantic HTML first, keyboard accessibility required, contrast ratios, visible labels, all states designed, CSS over JS for animations
- When given a task: 6-step process starting with user need, ending with keyboard/screen reader verification
  </action>
  <verify>
Read the file and confirm:
1. Frontmatter: name, model (sonnet), color, description (3 examples), tools (Documentation tier), permissionMode (default)
2. Examples cover accessibility, design systems, and responsive UI (not code implementation)
3. Tools are limited: `Read, Grep, Glob, Write, Edit, Bash` (no MultiEdit, NotebookEdit, WebFetch, WebSearch)
4. Model is `sonnet` (not inherit)
  </verify>
  <done>ux-designer.md is fully rewritten with professional description, 3 task examples covering UX/accessibility/design work, documentation-tier tools (no code editing power tools), sonnet model, and production-quality system prompt</done>
</task>

</tasks>

<verification>
After all 3 tasks complete:
1. Read all 3 files and verify frontmatter fields are present and correctly formatted
2. Verify no example scenario overlaps between javascript-developer and react-specialist
3. Verify tools field for each: javascript-developer and react-specialist have Implementation tier, ux-designer has Documentation tier
4. Count total description characters across all 3 agents (target: under 2500 chars total for the 3 agents, ~800 each)
</verification>

<success_criteria>
- All 3 frontend agent files have expanded descriptions with 3 `<example>` blocks each
- Tool restrictions are correctly applied per tier (Implementation for JS/React, Documentation for UX)
- ux-designer model is set to `sonnet`
- Delegation boundaries are clear: JS/TS/Node vs React/Next.js vs UX/accessibility
- System prompts are production quality with consistent three-section structure
</success_criteria>

<output>
After completion, create `.planning/phases/01-agent-hardening/01-01-SUMMARY.md`
</output>

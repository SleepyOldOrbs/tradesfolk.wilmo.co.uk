---
phase: 03-skills-refinement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/browse-pool/SKILL.md
  - skills/assemble-team/SKILL.md
autonomous: true
requirements:
  - SKIL-01
  - SKIL-03

must_haves:
  truths:
    - "browse-pool YAML frontmatter has no snake_case fields — only kebab-case or officially supported fields"
    - "assemble-team YAML frontmatter has no snake_case fields — uses argument-hint instead of args"
    - "browse-pool lists exactly 12 agents grouped by domain with updated descriptions from Phase 1"
    - "assemble-team Available specialists table lists exactly 12 agents with updated domain summaries from Phase 1"
    - "Both skills list the same 12 agents — no stale references, no missing agents"
  artifacts:
    - path: "skills/browse-pool/SKILL.md"
      provides: "Updated browse-pool skill with correct frontmatter and synced roster"
      contains: "name: browse-pool"
    - path: "skills/assemble-team/SKILL.md"
      provides: "Updated assemble-team skill with correct frontmatter and synced roster"
      contains: "argument-hint: task description"
  key_links:
    - from: "skills/browse-pool/SKILL.md"
      to: "agents/*.md"
      via: "hardcoded agent names matching agent filenames"
      pattern: "javascript-developer|react-specialist|ux-designer|python-developer|backend-architect|systems-programmer|database-specialist|qa-tester|security-auditor|devops-engineer|data-scientist|technical-writer"
    - from: "skills/assemble-team/SKILL.md"
      to: "agents/*.md"
      via: "Available specialists table matching agent filenames"
      pattern: "javascript-developer|react-specialist|ux-designer|python-developer|backend-architect|systems-programmer|database-specialist|qa-tester|security-auditor|devops-engineer|data-scientist|technical-writer"
---

<objective>
Fix frontmatter field names and sync agent roster in the existing browse-pool and assemble-team skills.

Purpose: The current skills use incorrect snake_case field names (`user_invocable`, `args`) that are not recognised by Claude Code. The agent descriptions are also stale — they predate the Phase 1 agent hardening rewrite. This plan fixes both issues so Claude Code correctly loads the skills and they reference the accurate 12-agent roster.

Output: Updated `skills/browse-pool/SKILL.md` and `skills/assemble-team/SKILL.md` with correct frontmatter and synced roster.
</objective>

<execution_context>
@/var/www/tradesfolk.wilmo.co.uk/.claude/get-shit-done/workflows/execute-plan.md
@/var/www/tradesfolk.wilmo.co.uk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-skills-refinement/03-RESEARCH.md
@.planning/phases/03-skills-refinement/03-CONTEXT.md
@skills/browse-pool/SKILL.md
@skills/assemble-team/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix browse-pool frontmatter and sync roster</name>
  <files>skills/browse-pool/SKILL.md</files>
  <action>
Rewrite `skills/browse-pool/SKILL.md` with these exact changes:

**Frontmatter** — replace the entire frontmatter block with:
```yaml
---
name: browse-pool
description: Browse the available specialist agent pool. Shows all agents with their expertise areas and recommended use cases.
---
```
Remove `user_invocable: true` entirely — `user-invocable` defaults to `true` so omitting is cleaner (per research, and "Use this to see who's available before assembling a team" trimmed from description to keep it concise).

**Body** — keep the same instructional structure but update the agent list under each domain category to include a brief domain summary from Phase 1 agent descriptions. Use this exact roster with these exact descriptions:

**Frontend & UI**
- `javascript-developer` — JS/TS implementation, Node.js backend, build tooling, code modernisation
- `react-specialist` — React components, Next.js architecture, server components, frontend performance
- `ux-designer` — Accessibility audits, design systems, responsive layouts, UI/UX patterns

**Backend & Systems**
- `python-developer` — Python web dev, API implementation, CLI tools, scripting
- `backend-architect` — API design, system architecture, service boundaries, technical decisions
- `systems-programmer` — Rust, Go, C/C++, performance-critical code, concurrency
- `database-specialist` — Schema design, query optimisation, migration planning, indexing

**Quality & Security**
- `qa-tester` — Test automation, test strategy, coverage analysis, E2E testing
- `security-auditor` — Code audits, threat modelling, vulnerability assessment, auth reviews

**Infrastructure & Operations**
- `devops-engineer` — CI/CD pipelines, containerisation, cloud infrastructure, IaC

**Data & ML**
- `data-scientist` — Data analysis, ML model training, experiment design, statistical methods

**Documentation**
- `technical-writer` — API documentation, developer guides, tutorials, ADRs

Keep the closing prompt: "Which specialists do you need for your task? I can help you assemble a team."

Keep the body format as instructions to Claude (same conversational style as current).
  </action>
  <verify>
1. Run: `head -5 skills/browse-pool/SKILL.md` — confirm frontmatter has `name:` and `description:` only (no `user_invocable`, no `args`)
2. Run: `grep -c 'user_invocable\|user_invokable\|args:' skills/browse-pool/SKILL.md` — must return 0
3. Run: `grep -c 'javascript-developer\|react-specialist\|ux-designer\|python-developer\|backend-architect\|systems-programmer\|database-specialist\|qa-tester\|security-auditor\|devops-engineer\|data-scientist\|technical-writer' skills/browse-pool/SKILL.md` — must return 12 (one per agent)
  </verify>
  <done>browse-pool has correct kebab-case-only frontmatter (no snake_case fields) and lists exactly 12 agents with updated Phase 1 descriptions, grouped by domain category</done>
</task>

<task type="auto">
  <name>Task 2: Fix assemble-team frontmatter and sync roster</name>
  <files>skills/assemble-team/SKILL.md</files>
  <action>
Rewrite `skills/assemble-team/SKILL.md` with these exact changes:

**Frontmatter** — replace the entire frontmatter block with:
```yaml
---
name: assemble-team
description: Assemble a team of specialist agents from the pool for a specific task. Describe the task and this skill will recommend which specialists to pull in.
argument-hint: task description
---
```
Remove `user_invocable: true` (defaults to true, and field name was wrong). Change `args: task_description` to `argument-hint: task description` (official field name, plain text value — no brackets per YAML parsing bug noted in research).

**Body — Available specialists table** — replace the current table with the updated 12-agent roster using Phase 1 descriptions. Use this exact table:

```markdown
| Agent | Domain |
|-------|--------|
| javascript-developer | JS/TS implementation, Node.js backend, build tooling |
| react-specialist | React components, Next.js architecture, server components |
| ux-designer | Accessibility audits, design systems, responsive layouts |
| python-developer | Python web dev, API implementation, CLI tools |
| backend-architect | API design, system architecture, service boundaries |
| systems-programmer | Rust, Go, C/C++, performance-critical code, concurrency |
| database-specialist | Schema design, query optimisation, migration planning |
| qa-tester | Test automation, test strategy, coverage analysis, E2E testing |
| security-auditor | Code audits, threat modelling, vulnerability assessment |
| devops-engineer | CI/CD pipelines, containerisation, cloud infrastructure |
| data-scientist | Data analysis, ML model training, experiment design |
| technical-writer | API documentation, developer guides, tutorials, ADRs |
```

Keep all other body content unchanged: the "You are the team assembly coordinator" intro, the Process section, the Team sizing guidelines section, and the closing "Shall I assemble this team?" prompt.
  </action>
  <verify>
1. Run: `head -6 skills/assemble-team/SKILL.md` — confirm frontmatter has `name:`, `description:`, `argument-hint:` only
2. Run: `grep -c 'user_invocable\|args:' skills/assemble-team/SKILL.md` — must return 0
3. Run: `grep 'argument-hint' skills/assemble-team/SKILL.md` — must show `argument-hint: task description`
4. Run: `grep -c 'javascript-developer\|react-specialist\|ux-designer\|python-developer\|backend-architect\|systems-programmer\|database-specialist\|qa-tester\|security-auditor\|devops-engineer\|data-scientist\|technical-writer' skills/assemble-team/SKILL.md` — must return 12 (one per agent)
  </verify>
  <done>assemble-team has correct frontmatter (argument-hint instead of args, no snake_case fields) and its Available specialists table lists exactly 12 agents with updated Phase 1 domain descriptions</done>
</task>

</tasks>

<verification>
1. Neither skill file contains any snake_case frontmatter fields (`user_invocable`, `args`)
2. Both skills list exactly the same 12 agents — cross-reference agent names between browse-pool and assemble-team
3. Every agent name matches an actual file in `agents/` directory: `ls agents/*.md | sed 's|agents/[0-9]*-||;s|\.md||'` should produce the same 12 names
4. Frontmatter YAML is valid: no indentation errors, no bracket/colon issues in values
</verification>

<success_criteria>
- browse-pool frontmatter: only `name` and `description` fields, no snake_case
- assemble-team frontmatter: `name`, `description`, and `argument-hint` fields, no snake_case
- Both skills list the same 12 agents with matching updated domain descriptions
- No stale agent references (old names, phantom agents, missing agents)
- Skill body content retains conversational style and instructional structure
</success_criteria>

<output>
After completion, create `.planning/phases/03-skills-refinement/03-01-SUMMARY.md`
</output>

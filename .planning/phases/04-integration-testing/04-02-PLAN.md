---
phase: 04-integration-testing
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - .planning/phases/04-integration-testing/04-TEST-REPORT.md
  - .planning/phases/04-integration-testing/04-02-SUMMARY.md
autonomous: false
requirements:
  - TEST-01
  - TEST-02

must_haves:
  truths:
    - "Plugin loads via --plugin-dir and Claude can see the agents"
    - "Skills are invocable via --plugin-dir in print mode and return useful output"
    - "Plugin installation is tested and documented (either working or with clear explanation of limitations)"
    - "Live session test instructions exist as copy-pasteable steps for manual verification"
    - "Test report exists with pass/fail evidence for every check"
  artifacts:
    - path: ".planning/phases/04-integration-testing/04-TEST-REPORT.md"
      provides: "Complete test report with automated and live session results"
      contains: "## Automated Validation Results"
  key_links:
    - from: ".claude-plugin/plugin.json"
      to: "claude --plugin-dir"
      via: "plugin loading mechanism"
      pattern: "plugin-dir"
    - from: "skills/browse-pool/SKILL.md"
      to: "claude -p /browse-pool"
      via: "skill invocation in print mode"
      pattern: "browse-pool"
---

<objective>
Test plugin installation and runtime behavior via `--plugin-dir`, attempt live session testing, and produce the comprehensive test report.

Purpose: Structural validation (Plan 01) proves the files are correct. This plan proves the plugin actually works at runtime -- agents load, skills execute, and installation mechanisms function. The test report is the deliverable that proves Phase 4 success criteria are met.

Output: Complete test report at `.planning/phases/04-integration-testing/04-TEST-REPORT.md` with pass/fail evidence for every automated and live test.
</objective>

<execution_context>
@/home/dmzserver/.claude/get-shit-done/workflows/execute-plan.md
@/home/dmzserver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-integration-testing/04-CONTEXT.md
@.planning/phases/04-integration-testing/04-RESEARCH.md
@.planning/phases/04-integration-testing/04-01-SUMMARY.md
@.claude-plugin/plugin.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test plugin loading via --plugin-dir and skill invocation</name>
  <files>.planning/phases/04-integration-testing/04-TEST-REPORT.md</files>
  <action>
Test runtime plugin behavior using `claude --plugin-dir` in print mode (`-p`). This tests both TEST-01 (agents/skills work) and TEST-02 (plugin loads from directory).

**Plugin loading test (TEST-02):**
1. Run: `claude --plugin-dir /var/www/tradesfolk.wilmo.co.uk -p "What plugins are currently loaded? List any available subagents from plugins."`
2. Capture output. Look for evidence that:
   - The agent-pool plugin is recognized
   - Agent names from the pool appear (javascript-developer, react-specialist, etc.)
3. Record PASS/FAIL with evidence

**Skill invocation tests (TEST-01):**
4. Run: `claude --plugin-dir /var/www/tradesfolk.wilmo.co.uk -p "/browse-pool"` and capture output
   - Expected: 12-agent roster grouped by domain (Frontend, Backend, Quality, Security, Infrastructure, Data)
5. Run: `claude --plugin-dir /var/www/tradesfolk.wilmo.co.uk -p "/assemble-team build a REST API with authentication and database"` and capture output
   - Expected: Team recommendation with relevant agents (backend-architect, database-specialist, security-auditor, etc.)
6. Run: `claude --plugin-dir /var/www/tradesfolk.wilmo.co.uk -p "/team-templates"` and capture output
   - Expected: 7 pre-built team compositions

For each test, record: command run, expected output description, actual output (first 50 lines or relevant portion), and PASS/FAIL.

If `--plugin-dir` doesn't work or skills don't invoke in print mode, document the failure clearly and note it as a limitation of headless testing. Then write manual test instructions instead.

**Installation testing (TEST-02 continued):**
7. Check if `claude plugin install` accepts a local path: `claude plugin install /var/www/tradesfolk.wilmo.co.uk 2>&1`
   - If it fails (expected per research -- needs marketplace), document the error
8. Try adding to settings.json enabledPlugins if applicable, or document the proper installation method
9. If install succeeds, run `claude plugin list` to verify, then uninstall to leave clean
10. Document the recommended installation approach based on what actually works

Record all results structured for the test report.
  </action>
  <verify>
At minimum 4 test commands attempted (plugin load, browse-pool, assemble-team, team-templates).
Each test has documented output and PASS/FAIL result.
Installation approach is documented with evidence.
  </verify>
  <done>All runtime tests executed (or documented as not-feasible-in-headless with manual instructions). Plugin loading via --plugin-dir tested. All 3 skills tested for invocation. Installation mechanism tested and documented.</done>
</task>

<task type="auto">
  <name>Task 2: Compile test report with automated and live results</name>
  <files>.planning/phases/04-integration-testing/04-TEST-REPORT.md</files>
  <action>
Create the comprehensive test report at `.planning/phases/04-integration-testing/04-TEST-REPORT.md`. This is the primary deliverable of Phase 4.

**Report structure:**

The report MUST contain these sections in order:

1. **Header** with date, plugin version, Claude Code version, environment
2. **Summary** with X/Y tests passed, Z deferred to manual testing
3. **Automated Validation Results** (from Plan 01) with test cases T-01 through T-07:
   - T-01: Plugin manifest validation (`claude plugin validate`)
   - T-02: Plugin.json required fields
   - T-03: Agent file count (12 expected)
   - T-04: Agent frontmatter validation (all 12 agents)
   - T-05: Skill directory and frontmatter validation (3 skills)
   - T-06: hooks.json structure
   - T-07: teammate-checklist.sh execution with test input
4. **Runtime Tests** (from Task 1) with test cases T-08 through T-12:
   - T-08: Plugin loads via --plugin-dir
   - T-09: /browse-pool skill invocation
   - T-10: /assemble-team skill invocation
   - T-11: /team-templates skill invocation
   - T-12: Plugin installation method
5. **Live Session Tests** section:
   - If tested interactively, show results
   - If not feasible on headless server, provide copy-pasteable Manual Test Instructions:
     a. Enable Agent Teams: `export CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`
     b. Start session: `claude --plugin-dir /var/www/tradesfolk.wilmo.co.uk`
     c. Test agent discovery (what to type, what to look for)
     d. Test `<example>` block routing (specific task to give, which agent should be selected)
     e. Test TeammateIdle hook (how to trigger, what to check in stderr)
   - List items deferred to manual testing
6. **Fixes Applied During Testing** (e.g., plugin.json agents field removal)
7. **Recommendations** for Phase 5

Each test case uses the evidence format: Command, Expected, Actual, Result (PASS/FAIL). If FAIL, include a "Fix needed" note.

Populate all sections with actual results from Plan 01 (reference the 04-01-SUMMARY.md) and Task 1 of this plan.

The manual test instructions section MUST be copy-pasteable. Include the specific `<example>` block validation concern from Phase 1 (do agents actually receive and use their example blocks?).
  </action>
  <verify>
File exists at `.planning/phases/04-integration-testing/04-TEST-REPORT.md`.
Contains sections: Summary, Automated Validation Results, Runtime Tests, Live Session Tests (or Manual Test Instructions).
Every test has: command, expected, actual, result.
Manual test instructions are specific and copy-pasteable.
  </verify>
  <done>Test report is complete with all automated results documented, runtime test results included, and clear manual testing instructions provided for anything that requires an interactive session. The `<example>` block concern from Phase 1 is addressed in manual test instructions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User reviews test report</name>
  <what-built>Complete integration test report covering structural validation, runtime testing via --plugin-dir, and manual test instructions for live session features. The plugin.json was fixed (agents field format bug) and all automated checks were run.</what-built>
  <how-to-verify>
    1. Read the test report at `.planning/phases/04-integration-testing/04-TEST-REPORT.md`
    2. Review the pass/fail results for automated and runtime tests
    3. Check if there are any FAIL results that need attention
    4. If you have access to an interactive Claude Code session, try the manual test instructions:
       - `export CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`
       - `claude --plugin-dir /var/www/tradesfolk.wilmo.co.uk`
       - Try delegating a task and see if pool agents are offered
       - Try `/browse-pool`, `/assemble-team`, `/team-templates`
    5. Confirm the test report adequately covers TEST-01 and TEST-02 requirements
  </how-to-verify>
  <resume-signal>Type "approved" if the test report is satisfactory, or describe any issues that need fixing.</resume-signal>
</task>

</tasks>

<verification>
- Plugin loads via `--plugin-dir` (or failure is documented with explanation)
- Skills invoke and return expected output (or limitation documented)
- Installation method tested and documented
- Test report exists with evidence for every check
- Manual test instructions provided for interactive-only features
- `<example>` block concern addressed in manual test plan
</verification>

<success_criteria>
1. Test report exists at `.planning/phases/04-integration-testing/04-TEST-REPORT.md`
2. All automated structural checks passed (from Plan 01 evidence)
3. Runtime tests via `--plugin-dir` executed and documented
4. Installation approach tested and documented
5. Manual test instructions provided for live session features
6. User has reviewed and approved the test report
</success_criteria>

<output>
After completion, create `.planning/phases/04-integration-testing/04-02-SUMMARY.md`
</output>

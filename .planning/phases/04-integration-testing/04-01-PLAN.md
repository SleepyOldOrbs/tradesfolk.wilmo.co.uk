---
phase: 04-integration-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude-plugin/plugin.json
  - .planning/phases/04-integration-testing/04-01-SUMMARY.md
autonomous: true
requirements:
  - TEST-01
  - TEST-02

must_haves:
  truths:
    - "plugin.json passes `claude plugin validate` with zero errors"
    - "All 12 agent files exist and have valid frontmatter (name, description, model, color)"
    - "All 3 skill directories have SKILL.md files with valid frontmatter"
    - "hooks.json is valid JSON with TeammateIdle event configured"
    - "teammate-checklist.sh is executable, accepts test JSON input, exits 0, and logs to stderr"
  artifacts:
    - path: ".claude-plugin/plugin.json"
      provides: "Fixed plugin manifest (agents field removed or converted to array)"
      contains: "\"name\": \"agent-pool\""
  key_links:
    - from: ".claude-plugin/plugin.json"
      to: "agents/*.md"
      via: "auto-discovery from agents/ directory"
      pattern: "agents"
    - from: "hooks/hooks.json"
      to: "hooks/teammate-checklist.sh"
      via: "command reference with CLAUDE_PLUGIN_ROOT"
      pattern: "CLAUDE_PLUGIN_ROOT"
---

<objective>
Fix the critical plugin.json validation bug and run comprehensive automated structural validation of the complete plugin.

Purpose: The `agents` field in plugin.json uses string format (`"./agents/"`) but the Claude Code validator requires an array. This must be fixed before any installation or runtime testing can succeed. Once fixed, validate every structural component: plugin manifest, all 12 agents, all 3 skills, and the hook system.

Output: A fixed plugin.json that passes `claude plugin validate`, plus verified structural integrity of all plugin components. Results captured for the test report in Plan 02.
</objective>

<execution_context>
@/home/dmzserver/.claude/get-shit-done/workflows/execute-plan.md
@/home/dmzserver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-integration-testing/04-CONTEXT.md
@.planning/phases/04-integration-testing/04-RESEARCH.md
@.claude-plugin/plugin.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix plugin.json agents field and validate manifest</name>
  <files>.claude-plugin/plugin.json</files>
  <action>
Fix the CRITICAL `agents` field format in `.claude-plugin/plugin.json`. The field is currently `"agents": "./agents/"` (a string), but the Claude Code validator requires an array format. Per research, the simplest and safest fix is to **remove the `agents` field entirely** — auto-discovery from the `agents/` directory works without it. This avoids the known issue (GitHub #21598).

Specifically:
1. Read `.claude-plugin/plugin.json`
2. Remove the `"agents": "./agents/"` line entirely (auto-discovery handles agent loading from the `agents/` directory convention)
3. Keep the `"skills"` and `"hooks"` fields as-is (they use string format which is accepted)
4. Write the updated file, preserving all other fields (name, version, description, author, license, keywords)
5. Run `claude plugin validate /var/www/tradesfolk.wilmo.co.uk` and capture the output — it MUST exit 0 with no errors
6. Also run a Python JSON structure check to verify required fields exist: name, version, description

If `claude plugin validate` still fails after removing the `agents` field, try the alternative: convert to an array listing all 12 agent files (`["./agents/01-javascript-developer.md", "./agents/02-react-specialist.md", ...]`). Document whichever approach succeeds.
  </action>
  <verify>
Run `claude plugin validate /var/www/tradesfolk.wilmo.co.uk` — must exit 0 with no errors.
Run `python3 -c "import json; d=json.load(open('/var/www/tradesfolk.wilmo.co.uk/.claude-plugin/plugin.json')); assert all(k in d for k in ['name','version','description']), 'Missing fields'; print('PASS')"` — must print PASS.
  </verify>
  <done>plugin.json passes `claude plugin validate` cleanly (exit 0, no error output). JSON is valid with required fields present. The agents field is either removed (relying on auto-discovery) or converted to array format.</done>
</task>

<task type="auto">
  <name>Task 2: Run full automated structural validation of all plugin components</name>
  <files>agents/*.md, skills/*/SKILL.md, hooks/hooks.json, hooks/teammate-checklist.sh</files>
  <action>
Run inline bash validation checks for every plugin component. These are one-time checks (NOT a permanent test suite per locked decision). Capture all output for the test report.

**Agent validation (all 12 files):**
- Iterate over `agents/*.md` files
- For each file, extract YAML frontmatter (between `---` markers)
- Verify 4 required fields exist: `name`, `description`, `model`, `color`
- Verify total count is exactly 12
- Verify each `model` value is one of: `inherit`, `sonnet`, `opus`, `haiku`
- Verify each `color` value is one of: `blue`, `cyan`, `green`, `yellow`, `magenta`, `red`

**Skill validation (3 directories):**
- Verify `skills/browse-pool/SKILL.md` exists and has `name` and `description` in frontmatter
- Verify `skills/assemble-team/SKILL.md` exists and has `name` and `description` in frontmatter
- Verify `skills/team-templates/SKILL.md` exists and has `name`, `description`, and `disable-model-invocation: true` in frontmatter

**Hook validation:**
- Verify `hooks/hooks.json` is valid JSON (use `python3 -c "import json; json.load(open(...))"`)
- Verify it contains a `TeammateIdle` key under `hooks`
- Verify the command references `CLAUDE_PLUGIN_ROOT`
- Verify `hooks/teammate-checklist.sh` has executable permission (`test -x`)
- Run the hook script with test JSON input: `{"session_id":"test-123","hook_event_name":"TeammateIdle","teammate_name":"javascript-developer","team_name":"test-team","cwd":"/tmp","permission_mode":"default"}`
- Verify exit code is 0
- Verify stderr output contains `[agent-pool] Teammate javascript-developer going idle`

Print a clear PASS/FAIL summary for each check. All checks must pass.
  </action>
  <verify>
All automated checks print PASS. Zero FAIL results. Specifically:
- 12 agents checked, 0 frontmatter failures
- 3 skills checked, 0 frontmatter failures
- hooks.json valid JSON with TeammateIdle configured
- teammate-checklist.sh exits 0 with expected stderr output
  </verify>
  <done>Every structural validation check passes. All 12 agents have valid frontmatter. All 3 skills have valid frontmatter. hooks.json is correctly structured. The hook script runs successfully with test input. Output is captured for inclusion in the test report.</done>
</task>

</tasks>

<verification>
- `claude plugin validate /var/www/tradesfolk.wilmo.co.uk` exits 0
- All 12 agent files have valid YAML frontmatter with name, description, model, color
- All 3 skill SKILL.md files have valid frontmatter
- hooks.json is valid JSON with TeammateIdle event
- teammate-checklist.sh is executable and runs correctly with test input
</verification>

<success_criteria>
1. plugin.json validation passes cleanly (the critical blocker is resolved)
2. All 12 agents, 3 skills, and hook system pass structural validation
3. Zero FAIL results across all automated checks
</success_criteria>

<output>
After completion, create `.planning/phases/04-integration-testing/04-01-SUMMARY.md`
</output>

---
phase: 09-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/09-verification/09-VERIFICATION.md
autonomous: true
requirements:
  - VRFY-01
  - VRFY-02
  - VRFY-03
must_haves:
  truths:
    - "All 20 agent files have valid YAML frontmatter with all 6 required fields (name, model, color, tools, permissionMode, description)"
    - "Each agent description contains at least 1 <example> block"
    - "Each agent body contains three-section system prompt structure"
    - "Total description payload across all 20 agents is measured and documented under 50,000 characters"
    - "Colour assignments are identical across agent files, browse-pool skill, CLAUDE.md roster table, and README.md"
    - "Verification report contains per-agent character count breakdown sorted by size"
    - "Report ends with overall PASS/FAIL verdict"
  artifacts:
    - path: ".planning/phases/09-verification/09-VERIFICATION.md"
      provides: "Complete verification report with pass/fail verdicts and data tables"
      contains: "PASS"
  key_links:
    - from: "agents/*.md color field"
      to: "skills/browse-pool/SKILL.md"
      via: "colour-to-agent mapping matches"
      pattern: "color.*blue|green|yellow|red|cyan|magenta"
    - from: "agents/*.md color field"
      to: "CLAUDE.md roster table"
      via: "colour column matches agent frontmatter"
      pattern: "Blue|Green|Yellow|Red|Cyan|Magenta"
    - from: "agents/*.md color field"
      to: "README.md roster"
      via: "colour column matches agent frontmatter"
      pattern: "Blue|Green|Yellow|Red|Cyan|Magenta"
---

<objective>
Verify the expanded 20-agent plugin: validate structural integrity of all agent files, measure context budget, cross-check colour consistency across all reference files, and produce a comprehensive verification report.

Purpose: Confirm v1.1.0 is ship-ready by validating all three phase success criteria (VRFY-01, VRFY-02, VRFY-03) with documented evidence.
Output: 09-VERIFICATION.md with pass/fail verdicts, data tables, and overall status
</objective>

<execution_context>
@/home/dmzserver/.claude/get-shit-done/workflows/execute-plan.md
@/home/dmzserver/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-verification/09-CONTEXT.md
@.planning/phases/09-verification/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate structural integrity of all 20 agent files</name>
  <files>.planning/phases/09-verification/09-VERIFICATION.md</files>
  <action>
For each of the 20 agent files in `agents/`:

**1. Required YAML frontmatter fields** — Extract and validate these 6 fields exist and are non-empty:
- `name` (kebab-case, 3-50 chars)
- `model` (one of: inherit, sonnet, opus, haiku)
- `color` (one of: blue, cyan, green, yellow, magenta, red)
- `tools` (comma-separated tool names, non-empty)
- `permissionMode` (one of: default, acceptEdits, plan, dontAsk, bypassPermissions)
- `description` (non-empty, contains text)

**2. Example blocks** — Check each agent's YAML `description` field for at least 1 `<example>` block. Count them.

**3. Three-section system prompt** — Check the body (after closing `---`) for the three required sections. Look for headings or bold markers containing these concepts:
- Core expertise (or similar: "expertise", "core skills", "specialist in")
- Working standards (or similar: "standards", "rules", "always", "never")
- When given a task (or similar: "task", "workflow", "approach", "steps")

Record per-agent results in a table:
| Agent | File | name | model | color | tools | permissionMode | description | examples | 3-section | Status |

Report any validation failures prominently.

**4. Auto-discovery readiness** — Document the `--plugin-dir` command for manual verification:
```
claude --plugin-dir /var/www/tradesfolk.wilmo.co.uk
```
Note that this confirms auto-discovery since plugin.json has no explicit `agents` field (auto-discovery was verified working in Phase 4 with 12 agents; the same mechanism discovers all 20).

Write the structural validation section to the verification report file.
  </action>
  <verify>
Read the verification report and confirm it contains a structural validation section with a 20-row table showing all agents, each with a PASS status for all 6 frontmatter fields plus example blocks and three-section prompt.
  </verify>
  <done>All 20 agent files validated: 6 required frontmatter fields present, at least 1 example block per agent, three-section system prompt structure confirmed. --plugin-dir command documented.</done>
</task>

<task type="auto">
  <name>Task 2: Measure context budget and cross-check colour consistency</name>
  <files>.planning/phases/09-verification/09-VERIFICATION.md</files>
  <action>
**Context Budget (VRFY-02):**

1. For each of the 20 agent files, extract the `description:` field from YAML frontmatter (this is what Claude Code loads for agent discovery/matching).
2. Count the character length of each description (excluding YAML syntax markers like `>`, `>-`, leading whitespace from folded scalars).
3. Create a per-agent table sorted by size (largest first):
| Rank | Agent | Characters |
4. Calculate total across all 20 agents.
5. Verdict: PASS if total < 50,000 characters; FAIL if >= 50,000.
6. If FAIL: report the overage amount but do NOT modify any files.

**Colour Consistency (VRFY-03):**

Extract agent-to-colour mapping from four sources:
1. **Agent files** (canonical): Parse `name` and `color` from each `agents/*.md` frontmatter
2. **browse-pool skill**: Parse `skills/browse-pool/SKILL.md` for agent-colour references
3. **CLAUDE.md**: Parse the roster table for agent names and colour column
4. **README.md**: Parse the roster table for agent names and colour column

Create a cross-reference table:
| Agent | Agent File | browse-pool | CLAUDE.md | README.md | Match? |

Mark any mismatches. If mismatches found:
- Fix the non-canonical file(s) to match the agent file colour
- Document what was changed in the report

Also validate colour-to-domain groupings follow CLAUDE.md conventions:
- Blue = Frontend & UI, Mobile & Platform
- Green = Backend & Systems
- Yellow = Quality
- Red = Security
- Cyan = Infrastructure & Operations
- Magenta = Data & ML, AI & ML Applications, Documentation

Append both sections (context budget and colour consistency) to the verification report.

**Overall Verdict:**

Add a final section with overall PASS/FAIL based on all three criteria:
- VRFY-01: Structural validation (from Task 1)
- VRFY-02: Context budget under 50K
- VRFY-03: Colour consistency across all files

Overall is PASS only if all three individual verdicts are PASS.
  </action>
  <verify>
Read the verification report and confirm:
1. Per-agent character count table exists with 20 rows sorted by size
2. Total character count is documented with PASS/FAIL verdict
3. Cross-reference colour table exists with 20 rows and 4 source columns
4. Any mismatches are documented and fixed
5. Overall verdict section exists at the end
  </verify>
  <done>Context budget measured and documented under 50K characters. Colour assignments verified identical across all four source files. Overall verification verdict recorded.</done>
</task>

</tasks>

<verification>
- [ ] 09-VERIFICATION.md exists at `.planning/phases/09-verification/09-VERIFICATION.md`
- [ ] Structural validation table has 20 rows, all showing PASS for 6 frontmatter fields
- [ ] Each agent has at least 1 example block confirmed
- [ ] Three-section system prompt structure confirmed for all 20 agents
- [ ] Per-agent character count table has 20 rows sorted by size
- [ ] Total description payload documented with numeric value and PASS/FAIL
- [ ] Colour cross-reference table has 20 rows with 4 source columns
- [ ] Any colour mismatches documented and fixed (or noted as all matching)
- [ ] Colour-to-domain groupings validated against CLAUDE.md conventions
- [ ] `--plugin-dir` command documented for manual testing
- [ ] Overall PASS/FAIL verdict present at end of report
- [ ] Report committed to git
</verification>

<success_criteria>
VRFY-01 complete: All 20 agents structurally validated for auto-discovery; --plugin-dir command documented.
VRFY-02 complete: Total description payload measured, per-agent breakdown provided, confirmed under 50K characters.
VRFY-03 complete: Colour assignments verified identical across agent files, browse-pool, CLAUDE.md, and README.md.
</success_criteria>

<output>
After completion, create `.planning/phases/09-verification/09-01-SUMMARY.md`
</output>
